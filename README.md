# boostio

## Building

Download the git submodules (only necessary on first run):
```bash
git submodule update --init --recursive
```

run cmake:
```bash
bash ./run_cmake.sh
```

build the project:
```bash
cd ./buid/Linux/
make
```


